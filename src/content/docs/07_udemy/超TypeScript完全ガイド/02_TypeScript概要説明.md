---
title: 02_TypeScript概要説明
---

# 🧩 TypeScriptとは？

## 一言で
>
> **「JavaScriptにコンパイルされる、静的型システムを持つJavaScriptの上位集合」**

---

## 1️⃣ コンパイルされる言語

### 🔹 意味

- TypeScriptは**直接実行されない**。
- 一度**JavaScriptに変換（コンパイル）**されてから、ブラウザやNode.jsで動く。

### 🔹 仕組み

```
TypeScript（.ts）
　↓ コンパイル
JavaScript（.js）
　↓ 実行
ブラウザ / Node.js
```

### 🔹 特徴

- JavaScriptが動く環境なら、TypeScriptも動かせる。
- 実際のコード変換は、**型注釈（例：: string）などが取り除かれる程度**。

---

## 2️⃣ 静的型システム（Static Type System）

### 🔹 意味

- TypeScriptはコンパイル時に**型検査（Type Check）**を行う。
- 変数・関数の引数・戻り値などが、正しい型かどうかを**事前に確認**。

### 🔹 メリット

- 実行前にエラーを検出できる（例：数値に文字列を代入など）。
- バグの早期発見・可読性向上・IDE補完が強力になる。

### 🔹 注意

- 型が合わないとエラーになる。
- JavaScriptでは通るコードでも、TypeScriptでは弾かれる場合がある。

---

## 3️⃣ JavaScriptの上位集合（Superset）

### 🔹 意味

- TypeScriptは**JavaScriptをすべて含む**（上位集合）。
- JavaScriptの構文はそのまま使える。
- TypeScriptは**型情報や新機能を追加した拡張版**。

### 🔹 図で表すと

```
+------------------------+
|       TypeScript       |
|  +------------------+  |
|  |   JavaScript     |  |
|  +------------------+  |
+------------------------+
```

- 青い部分＝TypeScript独自の追加要素（主に「型」）

---

## 4️⃣ 追加要素（TypeScript独自の拡張）

- 型注釈・インターフェース・ジェネリクスなど、**型に関する文法が中心**
- 一部、将来的にJavaScriptへ導入予定の新機能を先取りして採用

---

## ✅ まとめ（暗記フレーズ）

> TypeScriptとは、
> **「JavaScriptにコンパイルされる、静的型システム付きのJavaScript上位仕様」**
> である。

---

## 💡補足

| 用語 | 意味 |
|------|------|
| コンパイル | TS → JS に変換する処理 |
| 静的型検査 | 実行前に型エラーをチェック |
| 上位集合 | JSの機能をすべて含み、それに型の仕組みを足したもの |
| "use strict" | JSで厳密モードを有効化する記述。TSコンパイル後のJSにも自動付加される |

---

👩‍💻 **一言まとめ**
> TypeScript = 型が書ける強化版JavaScript 🚀
